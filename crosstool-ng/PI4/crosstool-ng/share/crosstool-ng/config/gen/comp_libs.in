#
# DO NOT EDIT! This file is automatically generated.
#

menuconfig COMP_LIBS_CLOOG
    bool "cloog"

if COMP_LIBS_CLOOG
config COMP_LIBS_CLOOG_PKG_KSYM
    string
    default "CLOOG"

source "config/versions/cloog.in"
source "config/comp_libs/cloog.in"
endif

menuconfig COMP_LIBS_EXPAT
    bool "expat"

if COMP_LIBS_EXPAT
config COMP_LIBS_EXPAT_PKG_KSYM
    string
    default "EXPAT"

source "config/versions/expat.in"
source "config/comp_libs/expat.in"
endif

menuconfig COMP_LIBS_GETTEXT
    bool "gettext"

if COMP_LIBS_GETTEXT
config COMP_LIBS_GETTEXT_PKG_KSYM
    string
    default "GETTEXT"

source "config/versions/gettext.in"
source "config/comp_libs/gettext.in"
endif

menuconfig COMP_LIBS_GMP
    bool "gmp"

if COMP_LIBS_GMP
config COMP_LIBS_GMP_PKG_KSYM
    string
    default "GMP"

source "config/versions/gmp.in"
source "config/comp_libs/gmp.in"
endif

menuconfig COMP_LIBS_GNUPRUMCU
    bool "gnuprumcu"
    depends on ARCH_PRU

if COMP_LIBS_GNUPRUMCU
config COMP_LIBS_GNUPRUMCU_PKG_KSYM
    string
    default "GNUPRUMCU"

source "config/versions/gnuprumcu.in"
source "config/comp_libs/gnuprumcu.in"
endif

menuconfig COMP_LIBS_ISL
    bool "isl"

if COMP_LIBS_ISL
config COMP_LIBS_ISL_PKG_KSYM
    string
    default "ISL"

source "config/versions/isl.in"
source "config/comp_libs/isl.in"
endif

menuconfig COMP_LIBS_LIBELF
    bool "libelf"

if COMP_LIBS_LIBELF
config COMP_LIBS_LIBELF_PKG_KSYM
    string
    default "LIBELF"

source "config/versions/libelf.in"
source "config/comp_libs/libelf.in"
endif

menuconfig COMP_LIBS_LIBICONV
    bool "libiconv"

if COMP_LIBS_LIBICONV
config COMP_LIBS_LIBICONV_PKG_KSYM
    string
    default "LIBICONV"

source "config/versions/libiconv.in"
source "config/comp_libs/libiconv.in"
endif

menuconfig COMP_LIBS_MPC
    bool "mpc"
    select MPFR_REQUIRE_older_than_4_0_0 if MPFR_NEEDED && !MPC_1_1_0_or_later

if COMP_LIBS_MPC
config COMP_LIBS_MPC_PKG_KSYM
    string
    default "MPC"

source "config/versions/mpc.in"
source "config/comp_libs/mpc.in"
endif

menuconfig COMP_LIBS_MPFR
    bool "mpfr"

if COMP_LIBS_MPFR
config COMP_LIBS_MPFR_PKG_KSYM
    string
    default "MPFR"

source "config/versions/mpfr.in"
source "config/comp_libs/mpfr.in"
endif

menuconfig COMP_LIBS_NCURSES
    bool "ncurses"

if COMP_LIBS_NCURSES
config COMP_LIBS_NCURSES_PKG_KSYM
    string
    default "NCURSES"

source "config/versions/ncurses.in"
source "config/comp_libs/ncurses.in"
endif

menuconfig COMP_LIBS_NEWLIB_NANO
    bool "newlib-nano"
    depends on BARE_METAL
    select LIBC_PROVIDES_CXA_ATEXIT
    help
      Newlib-nano is a C library intended for use on embedded systems. It is a
      conglomeration of several library parts, all under free software
      licenses that make them easily usable on embedded products. Newlib-nano
      is only available in source form. It can be compiled for a wide
      array of processors, and will usually work on any architecture with
      the addition of a few low-level routines.

if COMP_LIBS_NEWLIB_NANO
config COMP_LIBS_NEWLIB_NANO_PKG_KSYM
    string
    default "NEWLIB_NANO"

source "config/versions/newlib-nano.in"
source "config/comp_libs/newlib-nano.in"
endif

menuconfig COMP_LIBS_PICOLIBC
    bool "picolibc"
    depends on BARE_METAL && CONFIGURE_has_meson && CONFIGURE_has_ninja
    help
      This option adds Picolibc to an existing configuration which may have
      a C library, allowing you to install both and select between them
      when compiling applications using the toolchain
      Picolibc is a C library intended for use on embedded systems. It is a
      conglomeration of several library parts, all under BSD-compatible software
      licenses that make them easily usable on embedded products.

if COMP_LIBS_PICOLIBC
config COMP_LIBS_PICOLIBC_PKG_KSYM
    string
    default "PICOLIBC"

source "config/versions/picolibc.in"
source "config/comp_libs/picolibc.in"
endif

menuconfig COMP_LIBS_ZLIB
    bool "zlib"

if COMP_LIBS_ZLIB
config COMP_LIBS_ZLIB_PKG_KSYM
    string
    default "ZLIB"

source "config/versions/zlib.in"
source "config/comp_libs/zlib.in"
endif


config ALL_COMP_LIBS_CHOICES
    string
    default "CLOOG EXPAT GETTEXT GMP GNUPRUMCU ISL LIBELF LIBICONV MPC MPFR NCURSES NEWLIB_NANO PICOLIBC ZLIB"
