#
# DO NOT EDIT! This file is automatically generated.
#

# The component directory name
config BISON_DIR_NAME
    string
    default "bison"



config BISON_PKG_NAME
    string
    default "bison"

choice
    bool "Source of bison"

config BISON_SRC_RELEASE
    bool "Released tarball"
    help
      Download a released tarball.

config BISON_SRC_DEVEL
    bool "Vendor/custom repository"
    help
      Check out from a repository.
      Default is the vendor repository at https://git.savannah.gnu.org/git/bison.git

config BISON_SRC_CUSTOM
    bool "Custom location"
    depends on EXPERIMENTAL
    help
      Custom directory or tarball.

endchoice

if BISON_SRC_DEVEL

choice
    bool "VCS type"
    default BISON_DEVEL_VCS_git
    help
      Version control system from which the sources will be checked out.
      The default value points to the development repository for bison.

config BISON_DEVEL_VCS_git
    bool "Git"

config BISON_DEVEL_VCS_svn
    bool "Subversion"

config BISON_DEVEL_VCS_hg
    bool "Mercurial"

config BISON_DEVEL_VCS_cvs
    bool "CVS"

endchoice

config BISON_DEVEL_VCS
    string
    default "git" if BISON_DEVEL_VCS_git
    default "svn" if BISON_DEVEL_VCS_svn
    default "hg" if BISON_DEVEL_VCS_hg
    default "cvs" if BISON_DEVEL_VCS_cvs

config BISON_DEVEL_URL
    string "Repository URL"
    default "https://git.savannah.gnu.org/git/bison.git"
    help
      Repository URL.

      For CVS, enter both the value of CVS root and the module name, separated
      by a space.

config BISON_DEVEL_BRANCH
    string "Branch/tag to check out"
    default ""
    help
      Git/CVS: branch/tag to be checked out
      Subversion: directories to append to the repository URL (i.e. branch or tag)
      Mercurial: official guide recommends using separate repositories to maintain
      stable branches. You likely need to change the repository URL, rather than
      enter anything here.

config BISON_DEVEL_REVISION
    string "Revision/changeset"
    default ""
    help
      Commit ID or revision ID to check out.
      Git: enter the commit ID to check out a commit.
      CVS: enter the date in "YYYY/MM/DD HH:MM:SS" format (UTC) to check out certain date.
      Subversion: enter the revision.

config BISON_DEVEL_SUBDIR
    string "Subdirectory in the repository"
    default ""
    help
      Some projects produce releases not from the top-level directory in the
      repository, but rather from some subdirectory. If it is the case,
      specify this subdirectory here.

config BISON_DEVEL_BOOTSTRAP
    string "Bootstrap command"
    default "./bootstrap"
    help
      Command to run after checking out. Some projects don't store the generated
      files like configure script in the repository; building out of a checked out
      working copy thus requires some extra steps. Separate multiple shell commands
      with &&.

endif

if BISON_SRC_CUSTOM

config BISON_CUSTOM_LOCATION
    string "Custom source location"
    help
      Path to the directory or tarball with the sources.

endif

if EXPERIMENTAL
choice
    bool "bison patches origin"
    default BISON_PATCH_GLOBAL

config BISON_PATCH_GLOBAL
    bool "Per global policy"

config BISON_PATCH_BUNDLED
    bool "Bundled only"

config BISON_PATCH_LOCAL
    bool "Local only"
    select PATCH_USE_LOCAL

config BISON_PATCH_BUNDLED_LOCAL
    bool "Bundled, then local"
    select PATCH_USE_LOCAL

config BISON_PATCH_LOCAL_BUNDLED
    bool "Local, then bundled"
    select PATCH_USE_LOCAL

config BISON_PATCH_NONE
    bool "None"

endchoice
endif

config BISON_PATCH_ORDER
    string
    default "bundled" if BISON_PATCH_BUNDLED
    default "local" if BISON_PATCH_LOCAL
    default "bundled,local" if BISON_PATCH_BUNDLED_LOCAL
    default "local,bundled" if BISON_PATCH_LOCAL_BUNDLED
    default "none" if BISON_PATCH_NONE
    default "global"

choice
    bool "Version of bison"
    help
      For a released version, select the version of bison to download
      and build. For sources out of the vendor repository or from a custom
      location, select the version that describes those custom sources.
      Based on this version, crosstool-NG may apply some version-specific
      quirks while building bison.

config BISON_VERY_NEW
    bool "newer than anything below"
    depends on EXPERIMENTAL
    depends on BISON_SRC_DEVEL || BISON_SRC_CUSTOM

config BISON_V_3_5
    bool "3.5"

config BISON_V_3_4
    bool "3.4.2"

config BISON_V_3_3
    bool "3.3.2"

config BISON_V_3_2
    bool "3.2.4"

config BISON_V_3_1
    bool "3.1"

config BISON_V_3_0
    bool "3.0.5"

config BISON_VERY_OLD
    bool "older than anything above"
    depends on OBSOLETE && EXPERIMENTAL
    depends on BISON_SRC_DEVEL || BISON_SRC_CUSTOM

endchoice

# A flag indicating that no valid selections exist for the version.
# This may happen if there are conflicting requirements set by
# the host system and/or other packages.
config BISON_NO_VERSIONS
    bool
    select INVALID_CONFIGURATION
    default n if BISON_VERY_NEW
    default n if BISON_V_3_5
    default n if BISON_V_3_4
    default n if BISON_V_3_3
    default n if BISON_V_3_2
    default n if BISON_V_3_1
    default n if BISON_V_3_0
    default n if BISON_VERY_OLD
    default y

if BISON_NO_VERSIONS
comment "WARNING!"
comment "|  The bison package has no viable versions"
comment "|  due to conflicting constraints."
endif



config BISON_VERSION
    string
    default "3.5" if BISON_V_3_5
    default "3.4.2" if BISON_V_3_4
    default "3.3.2" if BISON_V_3_3
    default "3.2.4" if BISON_V_3_2
    default "3.1" if BISON_V_3_1
    default "3.0.5" if BISON_V_3_0
    default "new" if BISON_VERY_NEW
    default "old" if BISON_VERY_OLD
    default "unknown"

config BISON_MIRRORS
    string
    default "\$(CT_Mirrors GNU bison)"

config BISON_ARCHIVE_FILENAME
    string
    default "@{pkg_name}-@{version}"

config BISON_ARCHIVE_DIRNAME
    string
    default "@{pkg_name}-@{version}"

config BISON_ARCHIVE_FORMATS
    string
    default ".tar.xz .tar.gz"

config BISON_SIGNATURE_FORMAT
    string
    default "packed/.sig"




