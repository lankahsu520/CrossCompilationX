#
# DO NOT EDIT! This file is automatically generated.
#

# The component directory name
config PICOLIBC_DIR_NAME
    string
    default "picolibc"



config PICOLIBC_PKG_NAME
    string
    default "picolibc"

choice
    bool "Source of picolibc"

config PICOLIBC_SRC_RELEASE
    bool "Released tarball"
    help
      Download a released tarball.

config PICOLIBC_SRC_DEVEL
    bool "Vendor/custom repository"
    help
      Check out from a repository.
      Default is the vendor repository at https://github.com/picolibc/picolibc.git

config PICOLIBC_SRC_CUSTOM
    bool "Custom location"
    depends on EXPERIMENTAL
    help
      Custom directory or tarball.

endchoice

if PICOLIBC_SRC_DEVEL

choice
    bool "VCS type"
    default PICOLIBC_DEVEL_VCS_git
    help
      Version control system from which the sources will be checked out.
      The default value points to the development repository for picolibc.

config PICOLIBC_DEVEL_VCS_git
    bool "Git"

config PICOLIBC_DEVEL_VCS_svn
    bool "Subversion"

config PICOLIBC_DEVEL_VCS_hg
    bool "Mercurial"

config PICOLIBC_DEVEL_VCS_cvs
    bool "CVS"

endchoice

config PICOLIBC_DEVEL_VCS
    string
    default "git" if PICOLIBC_DEVEL_VCS_git
    default "svn" if PICOLIBC_DEVEL_VCS_svn
    default "hg" if PICOLIBC_DEVEL_VCS_hg
    default "cvs" if PICOLIBC_DEVEL_VCS_cvs

config PICOLIBC_DEVEL_URL
    string "Repository URL"
    default "https://github.com/picolibc/picolibc.git"
    help
      Repository URL.

      For CVS, enter both the value of CVS root and the module name, separated
      by a space.

config PICOLIBC_DEVEL_BRANCH
    string "Branch/tag to check out"
    default ""
    help
      Git/CVS: branch/tag to be checked out
      Subversion: directories to append to the repository URL (i.e. branch or tag)
      Mercurial: official guide recommends using separate repositories to maintain
      stable branches. You likely need to change the repository URL, rather than
      enter anything here.

config PICOLIBC_DEVEL_REVISION
    string "Revision/changeset"
    default ""
    help
      Commit ID or revision ID to check out.
      Git: enter the commit ID to check out a commit.
      CVS: enter the date in "YYYY/MM/DD HH:MM:SS" format (UTC) to check out certain date.
      Subversion: enter the revision.

config PICOLIBC_DEVEL_SUBDIR
    string "Subdirectory in the repository"
    default ""
    help
      Some projects produce releases not from the top-level directory in the
      repository, but rather from some subdirectory. If it is the case,
      specify this subdirectory here.

config PICOLIBC_DEVEL_BOOTSTRAP
    string "Bootstrap command"
    default ""
    help
      Command to run after checking out. Some projects don't store the generated
      files like configure script in the repository; building out of a checked out
      working copy thus requires some extra steps. Separate multiple shell commands
      with &&.

endif

if PICOLIBC_SRC_CUSTOM

config PICOLIBC_CUSTOM_LOCATION
    string "Custom source location"
    help
      Path to the directory or tarball with the sources.

endif

if EXPERIMENTAL
choice
    bool "picolibc patches origin"
    default PICOLIBC_PATCH_GLOBAL

config PICOLIBC_PATCH_GLOBAL
    bool "Per global policy"

config PICOLIBC_PATCH_BUNDLED
    bool "Bundled only"

config PICOLIBC_PATCH_LOCAL
    bool "Local only"
    select PATCH_USE_LOCAL

config PICOLIBC_PATCH_BUNDLED_LOCAL
    bool "Bundled, then local"
    select PATCH_USE_LOCAL

config PICOLIBC_PATCH_LOCAL_BUNDLED
    bool "Local, then bundled"
    select PATCH_USE_LOCAL

config PICOLIBC_PATCH_NONE
    bool "None"

endchoice
endif

config PICOLIBC_PATCH_ORDER
    string
    default "bundled" if PICOLIBC_PATCH_BUNDLED
    default "local" if PICOLIBC_PATCH_LOCAL
    default "bundled,local" if PICOLIBC_PATCH_BUNDLED_LOCAL
    default "local,bundled" if PICOLIBC_PATCH_LOCAL_BUNDLED
    default "none" if PICOLIBC_PATCH_NONE
    default "global"

choice
    bool "Version of picolibc"
    help
      For a released version, select the version of picolibc to download
      and build. For sources out of the vendor repository or from a custom
      location, select the version that describes those custom sources.
      Based on this version, crosstool-NG may apply some version-specific
      quirks while building picolibc.

config PICOLIBC_VERY_NEW
    bool "newer than anything below"
    depends on EXPERIMENTAL
    depends on PICOLIBC_SRC_DEVEL || PICOLIBC_SRC_CUSTOM
    select PICOLIBC_1_7_or_later
    select PICOLIBC_later_than_1_7
    depends on !PICOLIBC_REQUIRE_1_7_or_older
    depends on !PICOLIBC_REQUIRE_older_than_1_7
    select PICOLIBC_1_6_or_later
    select PICOLIBC_later_than_1_6
    depends on !PICOLIBC_REQUIRE_1_6_or_older
    depends on !PICOLIBC_REQUIRE_older_than_1_6
    select PICOLIBC_1_5_or_later
    select PICOLIBC_later_than_1_5
    depends on !PICOLIBC_REQUIRE_1_5_or_older
    depends on !PICOLIBC_REQUIRE_older_than_1_5
    select PICOLIBC_1_4_or_later
    select PICOLIBC_later_than_1_4
    depends on !PICOLIBC_REQUIRE_1_4_or_older
    depends on !PICOLIBC_REQUIRE_older_than_1_4

config PICOLIBC_V_1_7
    bool "1.7.6"
    depends on !PICOLIBC_REQUIRE_1_7_or_older
    select PICOLIBC_later_than_1_7
    depends on !PICOLIBC_REQUIRE_older_than_1_7
    select PICOLIBC_1_7_or_later
    depends on !PICOLIBC_REQUIRE_1_6_or_older
    select PICOLIBC_later_than_1_6
    depends on !PICOLIBC_REQUIRE_older_than_1_6
    select PICOLIBC_1_6_or_later
    depends on !PICOLIBC_REQUIRE_1_5_or_older
    select PICOLIBC_later_than_1_5
    depends on !PICOLIBC_REQUIRE_older_than_1_5
    select PICOLIBC_1_5_or_later
    depends on !PICOLIBC_REQUIRE_1_4_or_older
    select PICOLIBC_later_than_1_4
    depends on !PICOLIBC_REQUIRE_older_than_1_4
    select PICOLIBC_1_4_or_later

config PICOLIBC_V_1_6
    bool "1.6.2"
    depends on !PICOLIBC_REQUIRE_later_than_1_7
    select PICOLIBC_1_7_or_older
    depends on !PICOLIBC_REQUIRE_1_7_or_later
    select PICOLIBC_older_than_1_7
    depends on !PICOLIBC_REQUIRE_1_6_or_older
    select PICOLIBC_later_than_1_6
    depends on !PICOLIBC_REQUIRE_older_than_1_6
    select PICOLIBC_1_6_or_later
    depends on !PICOLIBC_REQUIRE_1_5_or_older
    select PICOLIBC_later_than_1_5
    depends on !PICOLIBC_REQUIRE_older_than_1_5
    select PICOLIBC_1_5_or_later
    depends on !PICOLIBC_REQUIRE_1_4_or_older
    select PICOLIBC_later_than_1_4
    depends on !PICOLIBC_REQUIRE_older_than_1_4
    select PICOLIBC_1_4_or_later

config PICOLIBC_V_1_5
    bool "1.5.1"
    depends on !PICOLIBC_REQUIRE_later_than_1_7
    select PICOLIBC_1_7_or_older
    depends on !PICOLIBC_REQUIRE_1_7_or_later
    select PICOLIBC_older_than_1_7
    depends on !PICOLIBC_REQUIRE_later_than_1_6
    select PICOLIBC_1_6_or_older
    depends on !PICOLIBC_REQUIRE_1_6_or_later
    select PICOLIBC_older_than_1_6
    depends on !PICOLIBC_REQUIRE_1_5_or_older
    select PICOLIBC_later_than_1_5
    depends on !PICOLIBC_REQUIRE_older_than_1_5
    select PICOLIBC_1_5_or_later
    depends on !PICOLIBC_REQUIRE_1_4_or_older
    select PICOLIBC_later_than_1_4
    depends on !PICOLIBC_REQUIRE_older_than_1_4
    select PICOLIBC_1_4_or_later

config PICOLIBC_V_1_4
    bool "1.4.7"
    depends on !PICOLIBC_REQUIRE_later_than_1_7
    select PICOLIBC_1_7_or_older
    depends on !PICOLIBC_REQUIRE_1_7_or_later
    select PICOLIBC_older_than_1_7
    depends on !PICOLIBC_REQUIRE_later_than_1_6
    select PICOLIBC_1_6_or_older
    depends on !PICOLIBC_REQUIRE_1_6_or_later
    select PICOLIBC_older_than_1_6
    depends on !PICOLIBC_REQUIRE_later_than_1_5
    select PICOLIBC_1_5_or_older
    depends on !PICOLIBC_REQUIRE_1_5_or_later
    select PICOLIBC_older_than_1_5
    depends on !PICOLIBC_REQUIRE_1_4_or_older
    select PICOLIBC_later_than_1_4
    depends on !PICOLIBC_REQUIRE_older_than_1_4
    select PICOLIBC_1_4_or_later

config PICOLIBC_VERY_OLD
    bool "older than anything above"
    depends on OBSOLETE && EXPERIMENTAL
    depends on PICOLIBC_SRC_DEVEL || PICOLIBC_SRC_CUSTOM
    select PICOLIBC_1_7_or_older
    select PICOLIBC_older_than_1_7
    depends on !PICOLIBC_REQUIRE_1_7_or_later
    depends on !PICOLIBC_REQUIRE_later_than_1_7
    select PICOLIBC_1_6_or_older
    select PICOLIBC_older_than_1_6
    depends on !PICOLIBC_REQUIRE_1_6_or_later
    depends on !PICOLIBC_REQUIRE_later_than_1_6
    select PICOLIBC_1_5_or_older
    select PICOLIBC_older_than_1_5
    depends on !PICOLIBC_REQUIRE_1_5_or_later
    depends on !PICOLIBC_REQUIRE_later_than_1_5
    select PICOLIBC_1_4_or_older
    select PICOLIBC_older_than_1_4
    depends on !PICOLIBC_REQUIRE_1_4_or_later
    depends on !PICOLIBC_REQUIRE_later_than_1_4

endchoice

# A flag indicating that no valid selections exist for the version.
# This may happen if there are conflicting requirements set by
# the host system and/or other packages.
config PICOLIBC_NO_VERSIONS
    bool
    select INVALID_CONFIGURATION
    default n if PICOLIBC_VERY_NEW
    default n if PICOLIBC_V_1_7
    default n if PICOLIBC_V_1_6
    default n if PICOLIBC_V_1_5
    default n if PICOLIBC_V_1_4
    default n if PICOLIBC_VERY_OLD
    default y

if PICOLIBC_NO_VERSIONS
comment "WARNING!"
comment "|  The picolibc package has no viable versions"
comment "|  due to conflicting constraints."
endif



config PICOLIBC_VERSION
    string
    default "1.7.6" if PICOLIBC_V_1_7
    default "1.6.2" if PICOLIBC_V_1_6
    default "1.5.1" if PICOLIBC_V_1_5
    default "1.4.7" if PICOLIBC_V_1_4
    default "new" if PICOLIBC_VERY_NEW
    default "old" if PICOLIBC_VERY_OLD
    default "unknown"

config PICOLIBC_MIRRORS
    string
    default "https://github.com/picolibc/picolibc/releases/download/${CT_PICOLIBC_VERSION}"

config PICOLIBC_ARCHIVE_FILENAME
    string
    default "@{pkg_name}-@{version}"

config PICOLIBC_ARCHIVE_DIRNAME
    string
    default "@{pkg_name}-@{version}"

config PICOLIBC_ARCHIVE_FORMATS
    string
    default ".tar.xz"

config PICOLIBC_SIGNATURE_FORMAT
    string
    default ""




config PICOLIBC_later_than_1_7
    bool

config PICOLIBC_1_7_or_later
    bool

config PICOLIBC_1_7_or_older
    bool

config PICOLIBC_older_than_1_7
    bool

config PICOLIBC_REQUIRE_later_than_1_7
    bool

config PICOLIBC_REQUIRE_1_7_or_later
    bool

config PICOLIBC_REQUIRE_1_7_or_older
    bool

config PICOLIBC_REQUIRE_older_than_1_7
    bool

config PICOLIBC_later_than_1_6
    bool

config PICOLIBC_1_6_or_later
    bool

config PICOLIBC_1_6_or_older
    bool

config PICOLIBC_older_than_1_6
    bool

config PICOLIBC_REQUIRE_later_than_1_6
    bool

config PICOLIBC_REQUIRE_1_6_or_later
    bool

config PICOLIBC_REQUIRE_1_6_or_older
    bool

config PICOLIBC_REQUIRE_older_than_1_6
    bool

config PICOLIBC_later_than_1_5
    bool

config PICOLIBC_1_5_or_later
    bool

config PICOLIBC_1_5_or_older
    bool

config PICOLIBC_older_than_1_5
    bool

config PICOLIBC_REQUIRE_later_than_1_5
    bool

config PICOLIBC_REQUIRE_1_5_or_later
    bool

config PICOLIBC_REQUIRE_1_5_or_older
    bool

config PICOLIBC_REQUIRE_older_than_1_5
    bool

config PICOLIBC_later_than_1_4
    bool

config PICOLIBC_1_4_or_later
    bool

config PICOLIBC_1_4_or_older
    bool

config PICOLIBC_older_than_1_4
    bool

config PICOLIBC_REQUIRE_later_than_1_4
    bool

config PICOLIBC_REQUIRE_1_4_or_later
    bool

config PICOLIBC_REQUIRE_1_4_or_older
    bool

config PICOLIBC_REQUIRE_older_than_1_4
    bool

